language: android
jdk: openjdk8
android:
    components:
        - platform-tools
        - tools
        - build-tools-23.0.3
        - android-23

before_script:
    - cd android && bash gradlew assembleRelease
